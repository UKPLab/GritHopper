site_name: grithopper
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: grithopper
      handlers:
        python:
          options:
            docstring_style: numpy